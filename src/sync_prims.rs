struct SyncPrimitive {
    byte_storm: /** the current state of pytes between classical, partial, and q, for testing snapshots */
    thread_safe: dont_know_yet,
}

// Looking for truths in random probably needed
function delete_jira() {
    
}